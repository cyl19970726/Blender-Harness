# Asset Art Head-Neck Negative Fixture

Synthetic negative fixture for the dragon failure mode.

The `head_neck_closeup_board` exists but is marked `helper_overlay=true` and
`usable=false`; `check-artifacts.mjs` must reject it. This models the failure
where helper collars/sockets hide a visible head-neck-body seam.
